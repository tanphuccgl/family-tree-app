import 'package:flutter/material.dart';

class ButtonAddFieldInforamtionF0Root extends StatelessWidget {
  const ButtonAddFieldInforamtionF0Root({super.key});

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {},
      child: const Icon(Icons.add),
    );
  }
}
